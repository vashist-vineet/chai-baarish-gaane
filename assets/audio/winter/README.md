# Optional dedicated winter recordings

Winter mode currently reuses `../wind/storm-wind.mp3` through its own slower,
low-pass Web Audio layer. No missing file is referenced by the application.

For a richer future mix, provide seamless licensed recordings at:

- `winter-wind.mp3`
- `fireplace.mp3`

They are intentionally not referenced until real files are present.
